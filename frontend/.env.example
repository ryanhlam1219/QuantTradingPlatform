# Frontend environment variables are not used for Binance credentials.
# Binance US API credentials are now configured in backend/.env and
# authentication happens server-side via the SSE proxy endpoint.
